REQUIREMENTS
We want to implement an intelligent monitoring systems for Cases in our Org, wihch uses the following: a LWC, APEX, a Trigger and Custom Metadata.

The purpose of the exercise is building a dynamic timer, which:
   - calculates live activ time 
   - handles pauses
   - changes the Status automatically, with regards to configurable thresholds
   - saves audit trail
   - reacts accordingly to resets and data changes

1. DATA MODEL
We need to have following custom fields on the Case object:

- SLA_Active__c (Checkbox; Sys. Admin visible)
- SLA_Start__c (DateTime; visible to Sys Admin, and not in Case Page Layout, so hidden)
- SLA_Paused__c (Checkbox; Sys. Admin visible)
- Pause_Start__c (DateTime; read-only; Sys. Admin visible)
- SLA_Total_Paused_Minutes__c (Number; Sys. Admin visible)
- SLA_Status__c (Picklist: SLA_Status Picklist Value Set: Inactive, On Track, Warning, Breached; Sys. Admin visible)

We also need:

Custom Metadata Type:
- SLA_Config__mdt
  - Warning_Hours__c (Number)
  - Breach_Hours__c (Number)

We create this Custom Metadata Type, to avoid hardcoding values into our implementation, so that thresholds can be easily modified and the code is dynamic.

We first start with the Trigger on Case. We want to have a trigger on the object Case, that is bulk-ready to activate and deactivate cases. In the trigger we check for the records if SLA_Active__c changes from "false" to "true" or viceversa. If yes we add them to the according List and for each List (activation and deactiavtion) we do the corresponding action. The action is designed as method in the Service class of the object Case, and takes as parameters a list of objects of type Case.

The activate/deactivate case methods are similar, checking to actually have a list of cases, then: 
    - for activation: 
         1. SLA_Start__c = NOW()
         2. SLA_Status__c = On Track
         3. SLA_Total_Paused_Minutes__c = 0
    - for deactivation:
         1. Reset all SLA fields
         2. SLA_Status__c = 'Inactive'
         3. LWC Timer must return to showing '00:00:00'

After having the Case Activation/Deactivation process handles, we can now start wokring the Timer LWC. 
In the HTML component we have a... (still need to add progress bar)

